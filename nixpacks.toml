[providers]
java = "maven"

[phases.setup]
nixPkgs = ["maven", "openjdk"]

[phases.build]
cmd = "cd backend && mvn clean package -DskipTests"

[phases.start]
cmd = "cd backend && java -jar target/school-health-management-0.0.1-SNAPSHOT.jar"

[variables]
NODE_ENV = ""
