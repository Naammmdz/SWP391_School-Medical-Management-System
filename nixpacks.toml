[providers]
java = "maven"

[phases.setup]
nixPkgs = ["maven", "openjdk"]

[phases.build]
cmd = "mvn clean package -DskipTests -f backend/pom.xml"

[phases.start]
cmd = "java -jar backend/target/school-health-management-0.0.1-SNAPSHOT.jar"

[variables]
NODE_ENV = ""
MAVEN_OPTS = "-Xmx1024m"
